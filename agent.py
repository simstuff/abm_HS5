import mesa
import numpy as np

class agent(mesa.Agent):
    def __init__(self, id, trust):
        self.id = id
        self.trust = {"agent_id":value} #initialize with np.random(), seed einfügen
        self.attributes = {"attr":value}
        self.wealth = #random int
        self.type =#trustee or trustor, changed by model

    def step():
        pass
    # choose agent
    # check money,
    # send money,
    # receive money, 
    # update trust value

    def connect():
        #connects to another agent in own network or outside of own network
        pass